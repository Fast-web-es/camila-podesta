import type { DashboardCategory } from '@/types';

// Camila's categories are configured here; the dashboard component is shared.
export const dashboardCategories: DashboardCategory[] = ['Retail', 'Interior', 'Art'];
