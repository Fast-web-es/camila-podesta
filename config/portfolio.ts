import { settings } from '@/content/portfolio';

export const portfolioId = process.env.NEXT_PUBLIC_PORTFOLIO_ID || 'camila';
export const portfolioName = process.env.NEXT_PUBLIC_PORTFOLIO_NAME || settings.name;
